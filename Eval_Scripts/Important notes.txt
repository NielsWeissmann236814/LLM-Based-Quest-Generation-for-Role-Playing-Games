CPU: AMD EPYC 9555 64-Core Processor
RAM: 1.5TB
GPU: 4 x NVIDIA RTX PRO 6000 Blackwell Server Edition

Metrics:

Structural validity — does the output conform to the expected format and scale?
Metrics: json_parse_success, num_quests, num_steps, scale_mismatch
 
Action validity — are the actions themselves legal and correctly formed?
Metrics: invalid_actions (action not in the action library), actor_not_player (wrong actor field), missing_parameters (required parameters absent), unknown_parameters (parameters that don't belong to that action)
 
World state consistency — do the steps respect the current state of the world?
Metrics: locked_move (moving into a locked location without unlocking first), move_noop (MOVE from X to X), hallucinated_locations, hallucinated_npcs, hallucinated_items (entities referenced that don't exist in the world state)
 
Inventory consistency — does the LLM correctly track what the player is carrying?
Metrics: pickup_no_move (picking up an item without being at its location), synth_no_sample (synthesizing cure without having picked up a cure sample), repair_no_toolkit and repair_no_wood (repairing without the required items), use_cure_no_synth (using antidote without having synthesized one)
 
Narrative coherence — does the quest make sense as a story?
Metrics: quest_no_talk (a quest with zero NPC interactions), duplicate_talk (consecutive TALK with the same NPC at the same location)
 
Performance — how efficiently did the model produce the output?
Metrics: generation_time_s, input_tokens, output_tokens, total_tokens, attempts (retries before valid output)

MODEL_CONFIGS = [
    {"name": "Qwen3.5-122B",    "base_url": "https://edirlei.com/buas-llm-server/v1", "api_key": BUAS_LLM_KEY},
    {"name": "Qwen3.6-27B",     "base_url": "https://edirlei.com/buas-llm-server/v1", "api_key": BUAS_LLM_KEY},
    {"name": "GPT-OSS-120B",    "base_url": "https://edirlei.com/buas-llm-server/v1", "api_key": BUAS_LLM_KEY},
    {"name": "Llama3.3-70B",    "base_url": "https://edirlei.com/buas-llm-server/v1", "api_key": BUAS_LLM_KEY},
    {"name": "claude-opus-4-7", "base_url": "https://edirlei.com/buas-llm-server/v1", "api_key": BUAS_LLM_KEY},
]